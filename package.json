{
  "name": "rc-balance",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.0",
  "scripts": {
    "dev": "wrangler dev",
    "db:migrate:local": "CI=1 wrangler d1 migrations apply DB --local",
    "db:migrate:remote": "CI=1 wrangler d1 migrations apply DB --remote",
    "deploy": "CI=1 wrangler d1 migrations apply DB --remote && wrangler deploy --config wrangler.toml",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260101.0",
    "typescript": "^5.7.3",
    "wrangler": "^4.56.0"
  }
}
